---
name: 消息 notify
route: /notify
---

import { Playground, PropsTable } from 'docz'
import Notify from '../components/notify/index.js'
import NotifyInner from '../components/notify/NotifyInner.js'
import Button from '../components/button/Button.js'

import '../components/notify/index.scss'

# 消息

## 代码演示

### 基本用法

<Playground>
  <Button 
    theme='primary' 
    outline
    onClick={
      () => Notify.open({
        status: 'success',
        message: '这是一条短消息',
        callback: () => console.log('短消息')
      })
    }
  >短消息</Button>
  <Button 
    theme='primary' 
    outline
    onClick={
      () => Notify.open({
        status: 'success',
        duration: 3000,
        message: '这是一条很长很长很长很长的消息'
      })
    }
  >长消息</Button>
</Playground>

### 自定义位置

<Playground>
  <Button 
    theme='primary' 
    outline
    onClick={
      () => Notify.open({
        status: 'success',
        message: '位置改变的消息',
        duration: 200000,
        position: {
          top: 100,
          right: 100,
        }
      })
    }
  >位置改变</Button>
</Playground>

## 属性

<PropsTable of={NotifyInner} />